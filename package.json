{
  "name": "@ben-elan/calcite-codemod",
  "version": "2.0.2",
  "description": "Codemod scripts for automatically updating applications after breaking changes to Calcite Design System packages",
  "keywords": [
    "calcite",
    "calcite-components",
    "calcite-design-system",
    "cli",
    "codemod",
    "node"
  ],
  "homepage": "https://github.com/benelan/calcite-codemod",
  "bugs": {
    "url": "https://github.com/benelan/calcite-codemod/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/benelan/calcite-codemod.git"
  },
  "license": "MIT",
  "author": "Ben Elan",
  "exports": {
    "import": "./dist/index.mjs",
    "require": "./dist/index.js",
    "default": "./dist/index.js"
  },
  "bin": {
    "calcite-codemod": "./dist/cli.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "npm run build:cjs && npm run build:esm",
    "build:cjs": "esbuild ./src/index.js ./src/cli.js --outdir=./dist --bundle --format=cjs --minify --platform=node",
    "build:esm": "esbuild ./src/index.js --outfile=./dist/index.mjs --bundle --format=esm --minify --platform=node",
    "postbuild": "npm run copy-assets",
    "copy-assets": "cp -r ./assets/legacy-tokens.css ./dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.19.8",
    "replace-in-file": "^7.0.2"
  },
  "volta": {
    "node": "20.10.0"
  }
}
